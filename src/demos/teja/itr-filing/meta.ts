import type { DemoMeta } from '../../types';

export default {
  title: 'ITR Filing',
  platform: 'mobile',
  note: 'Overview → filing flow, built from the mobile Taxes frames',
} satisfies DemoMeta;
